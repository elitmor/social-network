export type PostType = {
  id: string;
  message: string;
  likesCount: number;
};

export type DialogType = {
  id: string;
  name: string;
};

export type MessageType = {
  id: string;
  message: string;
};
